<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Login - SuperDry</title>
    <style>
        body {
            background: #f5f7fa;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }
        .admin-login-container {
            max-width: 400px;
            margin: 80px auto;
            background: #fff;
            padding: 36px 32px 28px 32px;
            border-radius: 12px;
            box-shadow: 0 4px 32px rgba(0,0,0,0.10);
        }
        .admin-logo {
            display: block;
            margin: 0 auto 24px auto;
            height: 100px;
            width: auto;
        }
        h2 {
            text-align: center;
            margin-bottom: 24px;
            font-weight: 400;
            color: #222;
        }
        .error-message {
            color: #e60000;
            text-align: center;
            margin-bottom: 1em;
        }
        .form-group {
            margin-bottom: 18px;
        }
        label {
            display: block;
            margin-bottom: 6px;
            font-weight: 500;
            color: #333;
        }
        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 6px;
            font-size: 1em;
        }
        .btn-submit {
            width: 100%;
            padding: 12px;
            background: #222;
            color: #fff;
            border: none;
            border-radius: 6px;
            font-weight: 600;
            font-size: 1em;
            cursor: pointer;
            margin-top: 10px;
        }
        .btn-submit:hover {
            background: #444;
        }
    </style>
</head>
<body>
    <div class="admin-login-container">
        <img src="/images/superdry.jpg" alt="SuperDry Logo" class="admin-logo">
        <h2>Admin Login</h2>
        <% if (typeof error !== 'undefined' && error) { %>
            <div class="error-message"><%= error %></div>
        <% } %>
        <form action="/admin/login" method="POST">
            <div class="form-group">
                <label for="email">Admin Email</label>
                <input type="email" id="email" name="email" required placeholder="admin@superdry.com">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required placeholder="••••••••">
            </div>
            <button type="submit" class="btn-submit">Login</button>
        </form>
    </div>
</body>
</html> 